**POJO** মানে 👉 **Plain Old Java Object** 🙂

একদম সোজা ভাষায় বললে—

> **POJO হলো সাধারণ Java class**, যেখানে
> ❌ কোনো framework-specific dependency নাই
> ❌ কোনো complex inheritance নাই
> ✅ শুধু data + getter/setter থাকে

---

## 🧠 POJO কী নয়?

POJO **হতে গেলে** এইগুলো **থাকা লাগবে না**:

* `extends HttpServlet`
* `implements Serializable` (mandatory না)
* `@Controller / @Service` (business role না)
* Container lifecycle logic

---

## ✅ POJO কী হয়?

সাধারণত থাকে:

* private fields
* getters / setters
* (optional) constructor
* (optional) `toString`, `equals`, `hashCode`

---

## 🔹 Simple POJO example

```java
public class User {
    private String name;
    private String email;

    // getter & setter
    public String getName() {
        return name;
    }
    public void setName(String name) {
        this.name = name;
    }
}
```

👉 এটিই একটি **POJO**।

---

## 🔹 POJO vs Entity vs DTO vs Config

| Type         | What it is                 | POJO?             |
| ------------ | -------------------------- | ----------------- |
| POJO         | Plain data class           | ✅                 |
| JPA Entity   | DB mapped class            | ✅ (but annotated) |
| DTO          | Data transfer object       | ✅                 |
| Config class | `@ConfigurationProperties` | ✅                 |

👉 Annotation থাকলেও class-টা **POJO nature হারায় না**

---

## 🔹 POJO in YAML → Java context

YAML:

```yml
api-gateway-token:
  user: admin
  password: secret
```

Java POJO:

```java
public class ApiGatewayTokenProperties {
    private String user;
    private String password;
}
```

Spring শুধু **data ঢুকিয়ে দেয়**, POJO নিজে কিছু করে না।

---

## 🏦 Enterprise explanation (interview-ready)

> “POJO stands for Plain Old Java Object. It is a simple Java class used to represent data without framework-specific behavior. In Spring Boot, POJOs are commonly used to bind configuration, transfer data, and model domain objects.”

---

## 🧠 Mental model

> **POJO = container (box)**
> **Spring = filler (who fills the box)**

---

## 🔚 Final takeaway

* POJO মানে **simple Java data class**
* Config mapping-এ POJO অপরিহার্য
* YAML যত complex → POJO structure তত clean

---

চাও তো next আমি দেখাতে পারি:

* 🧩 POJO vs Bean difference
* 🔍 POJO lifecycle in Spring
* 🧪 POJO validation (`@NotNull`, `@Validated`)

বললেই এগোই 🚀
