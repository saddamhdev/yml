সাদ্দাম, এখন তোমার structure অনুযায়ী **gateway-service run করার clean step-by-step guide** দিচ্ছি 👇

তোমার project:

```
base (parent)
 ├── gateway-service
 ├── gateway-service-env-properties
 ├── config-service
 └── user-service
```

---

# ✅ Step 1: Ensure Dependency Proper আছে

`gateway-service/pom.xml` এ এটা থাকতে হবে:

```xml
<dependency>
    <groupId>snvn</groupId>
    <artifactId>gateway-service-env-properties</artifactId>
    <version>${project.version}</version>
</dependency>
```

না থাকলে env YAML load হবে না ❌

---

# ✅ Step 2: Parent থেকে Build করো

Root folder এ যাও:

```
D:\module project\base
```

তারপর:

```bash
mvn clean install
```

এটা সব module build করবে।

---

# ✅ Step 3: Gateway Run করার 3টা উপায়

---

## 🔹 Option 1 — IntelliJ থেকে Run

1. `GatewayServiceApplication.java` open করো
2. Run button চাপো
3. VM option এ দিলে ভালো হয়:

```
-Dspring.profiles.active=dev
```

---

## 🔹 Option 2 — Maven দিয়ে

Root থেকে:

```bash
mvn -pl gateway-service spring-boot:run -Dspring.profiles.active=dev
```

---

## 🔹 Option 3 — Jar বানিয়ে Run

```bash
mvn clean package
```

তারপর:

```bash
cd gateway-service\target
java -jar gateway-service-0.0.1-SNAPSHOT.jar --spring.profiles.active=dev
```

---

# 🔥 Expected Console Output

ঠিকমতো হলে দেখবে:

```
Loaded config file 'classpath:application-dev.yml'
Netty started on port 8080
Started GatewayServiceApplication
```

---

# 🧪 Verify কাজ করছে কিনা

Browser এ যাও:

```
http://localhost:8080/actuator/health
```

যদি dependency থাকে:

```xml
<dependency>
  <groupId>org.springframework.boot</groupId>
  <artifactId>spring-boot-starter-actuator</artifactId>
</dependency>
```

---

# 🚨 Common Error Fix

### ❌ application-*.yml load হচ্ছে না?

✔ dependency check করো
✔ `spring.config.import` আছে কিনা দেখো
✔ debug mode চালাও:

```
--debug
```

---

### ❌ Port already in use?

application-dev.yml এ port change করো:

```yaml
server:
  port: 8085
```

---

# 🏗 তোমার Next Step (Important)

তুমি যেহেতু microservice বানাচ্ছো:

1. আগে user-service run করো
2. তারপর gateway run করো
3. route properly configure করো:

```yaml
spring:
  cloud:
    gateway:
      routes:
        - id: user
          uri: http://localhost:8081
          predicates:
            - Path=/users/**
```

---

# 🎯 Final Run Order (Microservice)

1️⃣ user-service
2️⃣ config-service (if needed)
3️⃣ gateway-service

---

যদি run না হয় — console error screenshot দাও
আমি debug করে exact fix বলবো 🔥

তুমি এখন full microservice orchestration phase এ আছো 😄
