চল এবার **live, end-to-end** করে দেখাই
👉 **YAML → Java Object binding**
(এটাই Spring Boot-এ সবচেয়ে গুরুত্বপূর্ণ জিনিসগুলোর একটা)

আমি একদম **real project style** দেখাবো, step-by-step।

---

# 🔁 YAML → Java Object Binding (Live Example)

---

## 🧩 Scenario (Real life)

ধরো, তোমার একটা **payment / third-party client** আছে
যার:

* base URL
* endpoint
* timeout

এই সব **code-এর বাইরে YAML-এ রাখতে চাও**।

---

## Step 1️⃣ YAML লিখি (application.yml)

```yml
payment:
  client:
    base-url: https://api.payment.com
    charge-uri: /charge
    refund-uri: /refund
    connect-timeout: 3000
    read-timeout: 5000
    enabled: true
```

👉 এটা pure **structured data**

---

## Step 2️⃣ Java Config Class বানাই

```java
package com.example.config;

import org.springframework.boot.context.properties.ConfigurationProperties;
import org.springframework.context.annotation.Configuration;

@Configuration
@ConfigurationProperties(prefix = "payment.client")
public class PaymentClientConfig {

    private String baseUrl;
    private String chargeUri;
    private String refundUri;
    private Integer connectTimeout;
    private Integer readTimeout;
    private boolean enabled;

    // getters & setters
}
```

🔥 এখানে magic হচ্ছে:

* `payment.client` → prefix
* kebab-case → camelCase auto map

---

## Step 3️⃣ Spring কীভাবে binding করে? (Inside magic)

Spring Boot startup-এ যা করে:

| YAML key                       | Java field     |
| ------------------------------ | -------------- |
| payment.client.base-url        | baseUrl        |
| payment.client.charge-uri      | chargeUri      |
| payment.client.refund-uri      | refundUri      |
| payment.client.connect-timeout | connectTimeout |
| payment.client.read-timeout    | readTimeout    |
| payment.client.enabled         | enabled        |

👉 **Type safe binding** হয় automatically

---

## Step 4️⃣ Config class use করি (LIVE usage)

```java
@Service
public class PaymentService {

    private final PaymentClientConfig config;

    public PaymentService(PaymentClientConfig config) {
        this.config = config;
    }

    public void pay() {
        if (!config.isEnabled()) {
            throw new RuntimeException("Payment disabled");
        }

        String url = config.getBaseUrl() + config.getChargeUri();
        System.out.println("Calling: " + url);
    }
}
```

👉 YAML change করলে → behavior change
👉 Code change লাগে না

---

## Step 5️⃣ Run করে prove করি (Console proof)

```java
@PostConstruct
public void test() {
    System.out.println(config.getBaseUrl());
    System.out.println(config.getConnectTimeout());
}
```

Output:

```
https://api.payment.com
3000
```

🔥 Binding successful

---

## Step 6️⃣ Profile wise YAML (Enterprise style)

### application-dev.yml

```yml
payment:
  client:
    base-url: https://dev.api.payment.com
```

### application-prod.yml

```yml
payment:
  client:
    base-url: https://api.payment.com
```

Spring automatically bind করবে।

---

## Step 7️⃣ YAML → Java binding rules (Golden rules)

### ✅ Naming

```yml
connect-timeout  → connectTimeout
read-timeout     → readTimeout
```

### ✅ Supported types

* String
* int / Integer
* boolean
* List
* Map
* Duration

---

## Step 8️⃣ List & Map binding example

### YAML

```yml
security:
  allowed-ips:
    - 10.0.0.1
    - 10.0.0.2

  headers:
    X-CLIENT-ID: ABC
    X-CHANNEL: WEB
```

### Java

```java
@ConfigurationProperties(prefix = "security")
public class SecurityConfig {

    private List<String> allowedIps;
    private Map<String, String> headers;
}
```

🔥 Clean & powerful

---

## Step 9️⃣ Why this is BETTER than `@Value`

❌ `@Value("${payment.client.base-url}")`
✔ messy
✔ not scalable

✅ `@ConfigurationProperties`
✔ grouped
✔ type safe
✔ enterprise standard

---

## Visual flow (YAML → Java)

---

## 🧠 Mental model (সবচেয়ে easy)

> **YAML = data**
> **Java class = box**
> **Spring Boot = auto filler**

Spring দেখে:

* prefix মিলছে
* নাম মিলছে
* type মিলছে

👉 box ভর্তি করে দেয় 😄

---

## Interview-ready one-liner 💼

> “Spring Boot automatically binds YAML configuration into type-safe Java objects using `@ConfigurationProperties`.”

---

পরের ধাপে চাইলে:

* 🔄 **CDN YAML → Java binding**
* 🔁 `@RefreshScope` live reload
* 🧪 Unit test for config class
* 🏦 Banking-grade config architecture

বললেই next level এ যাই 🚀
